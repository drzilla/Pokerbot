# Production report excerpt — safety-gated aggregate sizing leak

Generated by the **ordinary canonical pipeline**:
`PYTHONUTF8=1 GEM_ANALYST_MODE=0 python gem_analyzer.py "…/GEM 20260527/_session_live_test" Knockman`
→ wrote `/mnt/user-data/outputs/Pokerbot_Knockman_20260604-05_AUTO_ONLY_V1.{md,html}` (real June-04/05
session, 943 hands).

Call path that produced this section (all canonical, no per-hand candidates):

```
gem_analyzer __main__/analyze
  → GTO texture block: aggregate_compliance(eligible_hands,
        get_sizing_fn = _gto_sizing_pct  ← v8.21 gated by gem_sizing_detector.cbet_chart_applies)
  → stats['texture_gto_findings']
  → gem_coverage_builder.py:2205  build_sizing_leak_signals(stats['texture_gto_findings'])
  → report_data['sizing_leak_signals'] / ['sizing_leak_excluded']
  → gem_report_draft/draft.py:702  _emit_sizing_lines(...)  →  "## Sizing & Line Patterns"
```

Rendered section (text, tags stripped):

> ## Sizing & Line Patterns
>
> *Repeated bet-sizing patterns surfaced this session. Each is an AGGREGATE pattern across a whole board
> class — not a per-hand verdict; the example hands are evidence, not graded mistakes.*
>
> **Flop c-bets off-size on middling disconnected boards (IP)** — high confidence · aggregate
> **What:** Hero c-bet 33%, 60% on middling disconnected boards IP; the reference band is 100%/125%/150%.
> **Why it matters:** A repeated off-reference flop c-bet size leaks value or protection across this whole
> board class, not just one hand.
> **Adjustment:** On middling disconnected boards IP, size flop c-bets toward 100%/125%/150%.
> Sizing compliance **0.0% on 3 sized c-bets** · example hands: 3

## What this proves

- The **safety-gated** aggregate leak appears in an **ordinary generated report** (the same leak the
  deep-validation surfaced, now in the canonical surface).
- The gate is in effect: **"3 sized c-bets"** is the SRP/heads-up/non-all-in count for this session
  (ungated it would have judged more — the 3BP/multiway/all-in c-bets are counted in c-bet *frequency* but
  excluded from the *sizing* judgment).
- **Zero per-hand candidates, zero confirmed-mistake labels** — the surface explicitly states it is an
  aggregate pattern, "not a per-hand verdict … not graded mistakes."

(Note: the run logged a pre-existing report-nav QA warning `broken anchor links: ['sec-SL']`. It is a
TOC/anchor-link detail in the renderer, unrelated to the sizing gate — the section itself renders correctly.
Left untouched per "do not add features / reopen the pilot.")
